/**
 * QA automation layers (client, data, assertion, scenario, extension, cases).
 * Kept in the service test source set so Docker, CI {@code mvn verify}, and JaCoCo
 * stay on the existing single-module layout.
 */
package io.github.asyncflow.framework;
